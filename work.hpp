#include <iostream>
#include <string.h>
#include <cmath>
#include <stdlib.h>

using namespace std;

int testParam(int C, char **V);
int generate(int n);
char *division(int dividend, int devisor);
char *toBin(int num);
int deg(int num);
int toDec(char *bin);
